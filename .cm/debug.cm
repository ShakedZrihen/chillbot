requested_changes:
    if:
      - {{ pr.status == 'open' }}
      - {{ pr.requested_changes | length > 0 }}
    run:
      - action: add-label@v1
        args:
          label: '🚨 Changes requested'
          color: '#EC560D'
      - action: add-comment@v1
        args:
          comment: |
            change_reguests: {{ pr.requested_changes }}
