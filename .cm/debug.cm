# -*- mode: yaml -*-

manifest:
  version: 1.0

on:
  - label_added

triggers:
  exclude:
    branch:
      - "patch"
     
  

automations:
  checklist: 
    if:
      - true
    run:
      - action: add-comment@v1
        args:
          comment: {{ pr.labels | dump | safe }}
